# 홈 퍼널·성능 개선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 홈(`/`)을 "안내판"에서 "스스로 설득하는 한 장짜리 피치"로 바꾸고, 첫인상을 망치는 성능·안정성 결함을 제거한다.

**Architecture:** 콘텐츠는 이미 `/about`·칼럼에 잘 쓰여 있다. 새로 쓰기보다 **최고의 설득 자산(문제 공감 리스트·반전 후킹)을 홈으로 끌어올려 재배치**한다. 성능은 코드 변경 없이 이미지 경량화로, 안정성은 데이터 계층 try/catch로 해결한다. 새 의존성·새 테스트 프레임워크는 도입하지 않는다.

**Tech Stack:** Next.js 15(App Router)·React 19·TS, 승인된 시안 CSS(`app/globals.css`), Notion REST 연동, macOS `sips`(이미지 경량화).

## Global Constraints

- 새 의존성 추가 금지 — 현재 deps는 next/react/react-dom 3개뿐. 유지한다.
- 테스트 러너 없음 → 각 태스크 검증 = `npm run build` 통과 + 육안/`curl` 확인. 테스트 프레임워크 도입은 YAGNI(이 규모에 불필요).
- 디자인 토큰·기존 클래스(`.ab-pain`·`.ab-narrow`·`.hero-cta` 등)를 재사용한다. 새 CSS는 모바일 네비에만 추가.
- 카피는 백관장 보이스(직설·현실·경험기반) 유지. 숫자·고유명사·전자책 제목·가격·브랜드명은 변경 금지.
- 이미지 원본은 `assets/`에 보존돼 있음(profile.jpg·hyundai-id.png). `public/` 사본만 손댄다.
- 커밋 메시지는 한국어. 각 커밋 마지막 줄:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- 발행 빈도 주장("매주" 등) 금지 — 실제 발행 페이스 미확정.
- 전 작업 push 후 `CLAUDE.md` 진행 현황에 요약 1줄 추가(프로젝트 규칙).

---

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `public/profile.jpg`, `public/hyundai-id.png`→`.jpg` | 경량화된 이미지 | T1 |
| `app/page.tsx`, `app/about/page.tsx` | 이미지 ref(.png→.jpg) | T1 |
| `components/Header.tsx` | 모바일 햄버거 토글(client) | T2 |
| `app/globals.css` | 모바일 네비 드롭다운 CSS | T2 |
| `lib/notion.ts` | 데이터 실패 시 빈 폴백(try/catch) | T3 |
| `app/page.tsx` | 홈 퍼널 재배치(히어로 리드·문제 공감 블록·오리진 후킹·칼럼 프레이밍·종료 CTA) | T4 |

태스크 순서는 독립적이라 어느 것부터 해도 되지만, **T1·T2·T3(성능·안정성) → T4(퍼널)** 권장. T4가 가장 눈에 띄는 변경이라 마지막에 한 번에 검증.

---

### Task 1: 이미지 경량화 (성능 · P0)

첫 화면에 18.7MB(profile 14.8MB + hyundai-id 3.8MB)가 그대로 로드되는 문제. `sips`로 리사이즈/재인코딩하면 코드 변경 최소로 ~97% 감량. PNG는 사진이라 JPEG로 바꾼다(참조 2곳만 수정).

**Files:**
- Modify: `public/profile.jpg` (in place)
- Create: `public/hyundai-id.jpg` / Delete: `public/hyundai-id.png`
- Modify: `app/page.tsx:68`, `app/about/page.tsx:76`

**Interfaces:**
- Produces: `/profile.jpg`(경량), `/hyundai-id.jpg`(신규 경로). T4가 같은 경로를 그대로 참조.

- [ ] **Step 1: 작업 중 롤백용 백업(커밋 안 함)**

```bash
cp public/profile.jpg /tmp/profile.backup.jpg
cp public/hyundai-id.png /tmp/hyundai-id.backup.png
```

- [ ] **Step 2: profile.jpg 리사이즈·재인코딩 (긴 변 1400px, 품질 70)**

```bash
sips -Z 1400 -s formatOptions 70 public/profile.jpg --out public/profile.jpg
```
Expected: 14.8MB → 약 150~350KB. 확인:
```bash
ls -la public/profile.jpg && sips -g pixelWidth public/profile.jpg
```

- [ ] **Step 3: hyundai-id.png → 경량 JPEG 변환 (긴 변 1200px, 품질 72)**

```bash
sips -s format jpeg -Z 1200 -s formatOptions 72 public/hyundai-id.png --out public/hyundai-id.jpg
rm public/hyundai-id.png
ls -la public/hyundai-id.jpg
```
Expected: 3.8MB → 약 120~250KB.

- [ ] **Step 4: 코드 참조 .png → .jpg (2곳)**

`app/page.tsx:68`
```tsx
<img className="id-photo" src="/hyundai-id.jpg" alt="현대중공업 사원증" />
```
`app/about/page.tsx:76`
```tsx
            src="/hyundai-id.jpg"
```

- [ ] **Step 5: 남은 .png 참조 없는지 확인**

```bash
grep -rn "hyundai-id.png" app/ components/ lib/
```
Expected: 결과 없음(0건).

- [ ] **Step 6: 빌드 검증**

```bash
npm run build
```
Expected: 성공(에러 0). 이미지 깨진 참조 있으면 빌드는 통과해도 dev에서 확인:
```bash
npm run dev
# http://localhost:3000 와 /about 에서 두 사진 정상 표시 육안 확인
```

- [ ] **Step 7: 커밋**

```bash
git add public/profile.jpg public/hyundai-id.jpg app/page.tsx app/about/page.tsx
git commit -m "성능: 첫 화면 이미지 경량화(18.7MB→0.5MB 미만). profile 리사이즈, 사원증 PNG→JPEG 변환

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

> ⏭️ 업그레이드 경로(지금은 불필요): 더 줄이려면 `<img>`를 `next/image`로 교체(자동 WebP·반응형). 현재 `eslint-disable no-img-element` 주석도 그때 제거.

---

### Task 2: 모바일 네비게이션 복구 (UX · P0)

`globals.css`에서 `≤900px`일 때 `.menu{display:none}`인데 햄버거 대체물이 없어, 모바일에서 칼럼·상품·소개·문의로 갈 수 없다. SNS 유입=모바일이라 퍼널 동선이 끊김. Header를 client로 바꿔 토글 추가.

**Files:**
- Modify: `components/Header.tsx` (전체 교체)
- Modify: `app/globals.css:290` 부근(모바일 미디어쿼리)

**Interfaces:**
- Consumes: 없음. Produces: 동일한 `<Header active=... />` API 유지(호출부 변경 불필요).

- [ ] **Step 1: Header.tsx를 client + 토글로 교체**

`components/Header.tsx` 전체를 아래로 교체:
```tsx
"use client";

import Link from "next/link";
import { useState } from "react";

type Props = {
  active?: "columns" | "products" | "quiz" | "about" | "contact";
};

export default function Header({ active }: Props) {
  const [open, setOpen] = useState(false);
  return (
    <header>
      <div className="wrap nav">
        <Link className="brand" href="/" onClick={() => setOpen(false)}>
          {/* eslint-disable-next-line @next/next/no-img-element */}
          <img src="/logo.png" alt="백관장" /> 백관장의 체력 상담소
        </Link>
        <button
          className="nav-toggle"
          aria-label="메뉴"
          aria-expanded={open}
          onClick={() => setOpen((v) => !v)}
        >
          {open ? "✕" : "☰"}
        </button>
        <nav
          className={`menu ${open ? "open" : ""}`}
          onClick={() => setOpen(false)}
        >
          <Link href="/columns" className={active === "columns" ? "active" : undefined}>
            칼럼
          </Link>
          <Link href="/products" className={active === "products" ? "active" : undefined}>
            상품
          </Link>
          <Link href="/quiz" className={active === "quiz" ? "active" : undefined}>
            자가진단
          </Link>
          <Link href="/about" className={active === "about" ? "active" : undefined}>
            소개
          </Link>
          <Link href="/contact" className={active === "contact" ? "active" : undefined}>
            문의
          </Link>
        </nav>
        <Link className="nav-cta" href="/quiz">
          체력 자가진단 →
        </Link>
      </div>
    </header>
  );
}
```

- [ ] **Step 2: 데스크톱 토글 버튼 기본 숨김 CSS 추가**

`app/globals.css`의 `.nav-cta{...}`(49번째 줄 부근) 바로 아래에 추가:
```css
.nav-toggle{display:none;background:none;border:none;font-size:24px;
  cursor:pointer;color:var(--ink);line-height:1;padding:4px;}
```

- [ ] **Step 3: 모바일 미디어쿼리에서 `.menu{display:none}`을 드롭다운으로 교체**

`app/globals.css` 의 `@media(max-width:900px){ ... }` 블록 안에서 `.menu{display:none;}` 한 줄을 아래로 교체:
```css
  .nav-toggle{display:block;}
  .nav-cta{display:none;}
  .menu{
    display:none;position:absolute;top:72px;left:0;right:0;
    flex-direction:column;gap:0;background:#fff;
    border-bottom:1px solid var(--gray30);padding:8px 24px 16px;
    box-shadow:0 8px 16px rgba(0,0,0,.06);
  }
  .menu.open{display:flex;}
  .menu a{padding:14px 0;border-bottom:1px solid var(--gray10);font-size:16px;}
```
(`header`는 이미 `position:sticky`라 `.menu`의 `absolute`가 헤더 기준으로 앵커됨.)

- [ ] **Step 4: 빌드 + 모바일 폭 확인**

```bash
npm run build
```
Expected: 성공. 이어서:
```bash
npm run dev
# 브라우저 폭 ≤900px(또는 모바일 시뮬)에서: ☰ 클릭 → 5개 메뉴 드롭다운 표시 →
# 항목 클릭 시 닫힘. 데스크톱 폭에선 ☰ 안 보이고 기존 메뉴 그대로.
```

- [ ] **Step 5: 커밋**

```bash
git add components/Header.tsx app/globals.css
git commit -m "UX: 모바일 햄버거 메뉴 추가 — ≤900px에서 사라지던 네비게이션 복구

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 노션 장애 격리 (안정성 · P1)

`lib/notion.ts`의 데이터 함수가 try/catch 없이 throw → 노션 점검·토큰 만료 시 홈·칼럼이 전부 500. 칼럼 노출 때문에 사이트 전체 생존을 노션 가동률에 묶을 이유가 없다. 실패 시 빈 값 반환(홈엔 이미 "칼럼 준비 중" 폴백 존재, 상세는 `notFound()`로 흐름).

**Files:**
- Modify: `lib/notion.ts` (`getColumns`·`getColumnBySlug`·`getBlocks` 3개 함수)

**Interfaces:**
- Produces: `getColumns(): Promise<Column[]>` 실패 시 `[]`, `getColumnBySlug(): Promise<Column|null>` 실패 시 `null`, `getBlocks(): Promise<any[]>` 실패 시 `[]`. 호출부(홈·칼럼 목록·상세) 변경 불필요.

- [ ] **Step 1: `getColumns` try/catch**

`lib/notion.ts`의 `getColumns` 본문을 교체:
```ts
export async function getColumns(): Promise<Column[]> {
  try {
    const data = await notion(`data_sources/${DS_ID}/query`, {
      filter: { property: "상태", select: { equals: "발행완료" } },
      sorts: [{ property: "날짜", direction: "descending" }],
    });
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    return (data.results ?? []).map(mapColumn);
  } catch (e) {
    console.error("getColumns failed", e);
    return [];
  }
}
```

- [ ] **Step 2: `getColumnBySlug` try/catch**

```ts
export async function getColumnBySlug(slug: string): Promise<Column | null> {
  try {
    const data = await notion(`data_sources/${DS_ID}/query`, {
      filter: {
        and: [
          { property: "상태", select: { equals: "발행완료" } },
          { property: "slug", rich_text: { equals: slug } },
        ],
      },
    });
    const page = (data.results ?? [])[0];
    return page ? mapColumn(page) : null;
  } catch (e) {
    console.error("getColumnBySlug failed", e);
    return null;
  }
}
```

- [ ] **Step 3: `getBlocks` try/catch (루프 바깥 감싸기)**

```ts
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export async function getBlocks(pageId: string): Promise<any[]> {
  try {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const blocks: any[] = [];
    let cursor: string | undefined;
    do {
      const qs = cursor
        ? `?start_cursor=${cursor}&page_size=100`
        : `?page_size=100`;
      const data = await notion(`blocks/${pageId}/children${qs}`);
      blocks.push(...(data.results ?? []));
      cursor = data.has_more ? data.next_cursor : undefined;
    } while (cursor);
    return blocks;
  } catch (e) {
    console.error("getBlocks failed", e);
    return [];
  }
}
```

- [ ] **Step 4: 빌드 + 폴백 동작 확인**

```bash
npm run build
```
Expected: 성공. 폴백 시뮬(선택): `.env.local`의 `NOTION_TOKEN`을 일시적으로 잘못된 값으로 바꾸고 `npm run dev` → 홈이 500 대신 "칼럼을 준비 중입니다" 표시되는지 확인 후 토큰 원복.

- [ ] **Step 5: 커밋**

```bash
git add lib/notion.ts
git commit -m "안정성: 노션 API 실패 시 빈 폴백(try/catch) — 노션 장애가 사이트 전체를 죽이지 않도록

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 홈 퍼널 재배치 (전환 · 핵심)

홈이 "허브"라 콜드 트래픽 설득이 약하다. PAS(문제→공감→해결) 중 **문제 agitation**이 홈에 없고, 최고 자산(반전 후킹)이 `/about`에만 있다. 새 글쓰기 없이 재배치로 홈 한 장에서 설득이 완결되게 한다. CTA 위계는 **자가진단=주력(리드 캡처, P6 목적), 칼럼=부**(현재 히어로 위계 유지).

**Files:**
- Modify: `app/page.tsx` (5곳: 히어로 리드 · 문제 공감 블록 삽입 · 오리진 후킹 · 칼럼 프레이밍 · 종료 CTA)

**Interfaces:**
- Consumes: T1의 `/profile.jpg`·`/hyundai-id.jpg`. 기존 클래스 `.ab-narrow`·`.ab-h2`·`.ab-pain`·`.hero-cta`(globals.css에 이미 존재) 재사용. 새 CSS 없음.

- [ ] **Step 1: 히어로 리드를 "해법 먼저"→"아픔 먼저"로 (app/page.tsx:28-32)**

교체:
```tsx
            <p className="lead">
              퇴근하면 소파에 쓰러지는 하루. 의지가 약한 게 아닙니다.
              <br />
              방법이 틀렸을 뿐이에요. <b>운동량이 아니라 원리</b>입니다.
            </p>
```

- [ ] **Step 2: 히어로 `</section>` 직후, 오리진 `<section className="origin">` 앞에 문제 공감 블록 삽입**

`{/* ORIGIN STORY */}` 주석 줄 바로 위에 추가(`/about`의 검증된 카피 재사용, 기존 클래스만 사용):
```tsx
      {/* PAIN — 문제 공감 (about에서 끌어온 검증된 카피) */}
      <section className="block">
        <div className="wrap ab-narrow">
          <span className="eyebrow">혹시, 당신의 하루</span>
          <h2 className="ab-h2">혹시 이런 하루를 보내고 계신가요?</h2>
          <ul className="ab-pain">
            <li>알람을 다섯 개 맞춰놔도 아침이면 몸이 천근만근이다.</li>
            <li>점심 먹고 나면 오후 3시쯤 기절할 것 같은 식곤증이 몰려온다.</li>
            <li>퇴근하면 소파에 누워 스마트폰만 보다가 잠든다.</li>
            <li>주말 내내 쉬어도 월요일 아침엔 여전히 피곤하다.</li>
          </ul>
          <p>
            &ldquo;다들 이렇게 살지&rdquo; 싶겠지만, 정상이 아닙니다. 나이도
            의지도 체질도 아니에요. 하루 8시간 앉아 일하는 몸이라면 당연한
            결과입니다. <b>그리고 바꿀 수 있습니다.</b>
          </p>
        </div>
      </section>
```

- [ ] **Step 3: 오리진 본문에 반전 후킹 한 문단 추가 (app/page.tsx:78-86)**

기존 두 `<p>...</p>`를 아래 세 문단으로 교체(첫 문단이 후킹):
```tsx
            <p>
              파워리프팅 대회에서 1등을 했는데, 계단 5층만 올라도 숨이 찼습니다.
              그때 알았어요. 무거운 걸 드는 힘과, 일상을 버티는 체력은 전혀 다른
              겁니다.
            </p>
            <p>
              책상 앞에서 무너지는 체력, 퇴근하면 운동할 기운조차 없는 하루. 저도
              그 자리에 있었습니다. 그래서 &apos;직장인&apos;을 위한 체력 훈련을
              파고들었어요. 헬스장에서 몇 시간씩 보낼 필요 없이, 바쁜 일상 속에서도
              강해지는 원리를요.
            </p>
            <p>그게 백관장의 체력 상담소입니다.</p>
```

- [ ] **Step 4: 칼럼 섹션 프레이밍 + 발행 빈도 주장 제거 (app/page.tsx:106)**

교체:
```tsx
              <p>막막할 때 가장 먼저 읽을 글. 직장인에게 실제로 통한 체력 원리만 모았습니다.</p>
```

- [ ] **Step 5: 종료 CTA 밴드에 전환 액션 추가 (SNS만으로 끝나던 문제)**

`app/page.tsx`의 `<section className="cta-band" id="sns">` 안, `<p>유튜브 · 스레드 ...</p>` 다음·`<div className="sns">` 앞에 삽입:
```tsx
          <div
            className="hero-cta"
            style={{ justifyContent: "center", marginBottom: 36 }}
          >
            <Link
              className="btn"
              href="/quiz"
              style={{ background: "#fff", color: "#004AAD", borderColor: "#fff" }}
            >
              1분 체력 자가진단 →
            </Link>
            <Link
              className="btn"
              href="/products"
              style={{
                background: "transparent",
                color: "#fff",
                borderColor: "rgba(255,255,255,.5)",
              }}
            >
              상품 둘러보기
            </Link>
          </div>
```
(아래 `.sns` 소셜 링크는 부차 동선으로 그대로 둔다.)

- [ ] **Step 6: "매주" 잔존 없는지 확인**

```bash
grep -rn "매주" app/ components/
```
Expected: 결과 없음(0건).

- [ ] **Step 7: 빌드 + 홈 스크롤 육안 검증**

```bash
npm run build && npm run dev
# 홈 스크롤 순서 확인: 히어로(아픔 리드) → 문제 공감 리스트 → 오리진(반전 후킹) →
# 칼럼 → 후기 → 상품 → 종료 CTA(자가진단/상품 버튼) → SNS. 모바일 폭도 함께 확인.
```
Expected: 빌드 성공, 새 섹션 정상 렌더, 레이아웃 깨짐 없음.

- [ ] **Step 8: 커밋**

```bash
git add app/page.tsx
git commit -m "전환: 홈을 허브→피치로 재배치 — 문제 공감 블록·반전 후킹·종료 CTA 추가, 미검증 발행빈도 주장 제거

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 마무리 (전 태스크 후)

- [ ] **push + 진행 현황 갱신**

```bash
git push
```
이어 `CLAUDE.md` 진행 현황(작업 로그)에 1줄 추가:
`- 2026-06-26 — 성능(이미지 18.7MB→0.5MB)·모바일 네비 복구·노션 장애 격리 + 홈 퍼널 재배치(문제공감·반전후킹·종료CTA).`

- [ ] **라이브 검증(Vercel 자동배포 후)**

```bash
curl -sI https://masterbaek.vercel.app/profile.jpg | grep -i content-length   # 1MB 미만 확인
```
모바일에서 햄버거·홈 스크롤 동선 최종 확인.

---

## Self-Review

- **스펙 커버리지:** 1차 진단(이미지 18.7MB=T1 / 모바일 네비=T2 / 노션 throw=T3 / "매주" 미검증 주장=T4-S4)·퍼널 진단(문제 agitation 부재=T4-S2 / 해법먼저 리드=T4-S1 / 반전후킹 부재=T4-S3 / 종료 이탈=T4-S5 / 칼럼 프레이밍=T4-S4) 모두 태스크에 매핑됨. P2 항목(폰트 next/font·커스텀404·통증케어 vaporware)은 콘텐츠 결정이 섞여 **이번 계획에서 의도적 제외**(별도 진행 권장).
- **플레이스홀더:** 모든 코드 스텝에 실제 코드/명령·기대 출력 포함. "적절히 처리" 류 없음.
- **타입 일관성:** `getColumns/getColumnBySlug/getBlocks` 반환 타입 변경 없음(폴백 값만 추가). Header `Props` API 동일 유지. 이미지 경로 `/hyundai-id.jpg`로 T1·T4 일치.
- **의도적 단순화:** 테스트 프레임워크·next/image·error.tsx 미도입(YAGNI). 검증은 build+육안+curl.
