# CLAUDE.md — 백관장의 체력 상담소 (홈페이지 프로젝트)

> 이 파일은 작업 시 항상 따라야 할 규칙과 프로젝트 핵심 정보를 담는다.
> 상세 계획: [`구현계획.md`](./구현계획.md) · 미끼(자가진단): [`자가진단테스트_계획.md`](./자가진단테스트_계획.md)

---

## ⚠️ 작업 규칙 (필수)

1. **작업 시작 전** — ① `git pull`로 최신 상태 받기 ② 이 파일 맨 아래 **[진행 현황](#-진행-현황-작업-로그)** 확인하고 시작.
2. **작업 완료 후** — ① `git add` → `git commit` → `git push` ② **[진행 현황](#-진행-현황-작업-로그)에 이번 작업 요약 메모 추가** (날짜 포함).
3. 커밋 메시지는 한국어로 명확하게 (무엇을 왜 바꿨는지).
4. **한글 텍스트 발행 전 — 마지막에 윤문(`/humanize-korean`)** 적용해 AI 티 제거. (아래 "콘텐츠 윤문" 참고)
5. 발행/배포 등 되돌리기 어려운 작업은 진행 전 사용자에게 확인한다.

```bash
# 작업 시작
git pull            # + 진행 현황 확인

# 작업 완료
git add .
git commit -m "변경 내용 요약"
git push            # + 진행 현황에 요약 메모 추가
```

- GitHub: https://github.com/baekstrong/baekwebpage (`main` 브랜치)
- **배포 URL: https://masterbaek.vercel.app** (Vercel, `main` push 시 자동배포)

### 콘텐츠 윤문 (한글 발행 전 마지막 단계)
- 도구: [im-not-ai / Humanize KR](https://github.com/epoko77-ai/im-not-ai) — Claude Code 스킬 `/humanize-korean`
- 설치(1회): `/plugin marketplace add epoko77-ai/im-not-ai` → `/plugin install humanize-korean@im-not-ai`
- 적용 대상: 칼럼·자가진단 결과문·이메일 시퀀스·사이트 카피 등 모든 발행 한글
- 파이프라인: `초안(AI) → 감수(백관장) → ★윤문(/humanize-korean) → 발행`

---

## 프로젝트 개요

**무엇:** 백관장(백승덕)의 체력향상 콘텐츠 허브 + 상품 판매 퍼널 홈페이지
**벤치마킹:** https://awesomebliss.kr/

**목적 (퍼널):**
```
SNS 유입(유튜브·스레드·인스타·틱톡) → 칼럼으로 설득 → 상품 구매 전환
```
어썸블리스와 차이: **칼럼(콘텐츠)을 퍼널의 중심**에 둔다.

---

## 브랜드

- **사이트 브랜드명**: **백관장의 체력 상담소** (채널명 계승, 구독자 6.2만 인지도 활용)
  - ※ "근력학교"는 **정규수업 상품명**일 뿐, 사이트 브랜드명이 아님
- **운영자**: 백관장(백승덕) — **전직 현대중공업 엔지니어** (사원증 = 오리진 스토리 핵심 자산)
- **슬로건**: "직장인에게, 힘을!"
- **미션**: 스트렝스 트레이닝의 대중화
- **타깃**: 직장인 — 바쁜 와중에 체력/근력을 키우려는 사람
- **보이스**: 직설적·현실적·경험기반·친근하되 전문적 (군더더기 없음)
- **채널**: @easystrength101 (유튜브 구독 6.18만)

---

## 기술 스택 (확정)

| 영역 | 선택 |
|------|------|
| 프레임워크 | Next.js (App Router) + TypeScript |
| 스타일 | Tailwind CSS |
| 칼럼 CMS | **Notion** (기존 발행 흐름 그대로) |
| 배포 | **Vercel** (git push 자동배포, 무료) |
| 결제/판매 | **외부 플랫폼 링크** (자체 결제 X) |
| 도메인 | 없음 → Vercel 임시주소로 시작, 추후 구매 |

---

## 칼럼 자동 발행 구조 (핵심)

**사용자 작업 흐름은 바뀌지 않는다.** 지금처럼 Notion에 발행만 하면 된다.

```
원본(영상대본/메모)
  → marketingteamforbaek 에이전트가 백관장 톤 뉴스레터로 변환
  → Notion 발행용 DB(30f68eb32d9b809e8477f11b3b9b4a37) 발행
  → 상태: 검토 대기 → (사용자 감수) → 발행 완료   ← 감수 게이트
  → 홈페이지가 "발행 완료"만 자동으로 읽어옴 (ISR)
  → 사이트에 칼럼으로 노출
```

- Notion 속성 매핑: 제목→칼럼제목 / 날짜→정렬 / **상태=`발행 완료`만 노출** / 등급 A=홈 추천칼럼 / 본문→렌더링
- 별도 레포: https://github.com/baekstrong/marketingteamforbaek (뉴스레터 변환·발행)
- **콘텐츠 자산(이미 존재)**: 뉴스레터 ~18편(칼럼 1순위) + 블로그 ~60편(지역 SEO, 선별 활용)
- 사전 준비(사용자 1회): Notion Integration 토큰 발급 + 발행용 DB 연결 → Vercel 환경변수 저장

---

## 결제 플랫폼 (상품별 구매 버튼 목적지)

| 상품 | 결제 목적지 |
|------|------------|
| 전자책 | **리틀리** (litt.ly/easystrength) |
| 원데이 오프라인 강의 | **스마트스토어** |
| 근력학교 정규수업 | **스마트스토어** |
| 온라인 강의 (10편 이하) | 미정 |
| 운동 프로그램 | 미정 |
| 실물 굿즈 | 추후 |

---

## 디자인 시스템

- **컨셉**: 브랜드 남색 + IBM Carbon = "신뢰감 있고 군더더기 없는 전문가 사이트"
- **메인 색**: Brand Navy **`#004AAD`** (로고서 추출, 흰색 로고와 한 쌍)
  - 호버 `#003A87` / 액티브 `#002D6E` / 틴트 `#E6EEF8`
  - 텍스트 `#161616` / 보조 `#525252` / 보더 `#c6c6c6` / 면 `#f4f4f4` / 배경 `#fff`
  - 에너지 액센트(소량) Red `#DA1E28`
- **폰트**: IBM Plex Sans KR (무게 300/400/600 위주)
- **레이아웃**: 8px 그리드, 모서리 0px, 그림자 최소(배경 계층화), 컨테이너 ~1200px, 모바일 우선

---

## 사이트맵

```
/                  홈 (랜딩)
/columns           칼럼 목록 (Notion 자동 생성)
/columns/[slug]    칼럼 상세 (+ 하단 관련 상품 CTA)
/products          상품 전체
/products/[id]     상품 상세 (구매 → 외부 플랫폼)
/about             백관장 소개 (현대중공업 오리진 스토리)
/contact           문의 (이메일 / SNS)
```

---

## 자산 (assets/)

- `assets/logo.png` — 흰색 "백관장" 로고 (남색 배경)
- `assets/profile.jpg` — 백승덕 헬스장 프로필 사진
- `assets/hyundai-id.png` — 현대중공업 사원증 (오리진 스토리용)

---

## 구현 단계 (Phase)

| 단계 | 내용 |
|------|------|
| P0 | Next.js + Tailwind 셋업, Vercel 연결, 자동배포 확인 |
| P1 | 디자인 시스템 + 홈 랜딩 |
| P2 | Notion 칼럼 연동 (목록/상세, ISR 자동갱신) |
| P3 | 상품 목록/상세 + 외부 결제 링크 |
| P4 | 소개/문의 + SNS 링크 |
| P5 | SEO/OG/성능/도메인 연결 |
| P6 | 자가진단 테스트(미끼) + 이메일 수집/자동화 |

---

## 📌 진행 현황 (작업 로그)

> 작업 시작 시 여기부터 확인. 작업 종료 시 맨 위에 날짜와 요약을 추가.

**현재 단계:** ✅ **P2·P3·P5·P6 + 스티비 + 윤문 + 신뢰보강(SNS·SEO/OG·후기·신청URL) 완료** — 칼럼 연동·실제 5편(윤문완료) 발행, 상품 상세+오프라인 신청 링크 연결, 자가진단(스티비 실연동), 후기 섹션(네이버·스마트스토어 실후기), OG/사이트맵/문의 페이지 모두 라이브. **buyUrl TODO 전부 해소.** 남은 선택과제: 보안(노션·스티비 키 재발급), 상품 FAQ, 도메인.

> ✅ Vercel 환경변수 등록 완료(`NOTION_TOKEN`·`NOTION_COLUMNS_DS_ID` — Production·Development. Preview는 미등록, 필요시 대시보드에서 보충).
> ✅ 자동배포 연결됨 — `main` push 시 Vercel 프로덕션 자동배포(검증 통과).
> 🔁 운영: 노션 "홈페이지 칼럼" DB에서 글을 `발행완료`로 바꾸면 ISR(60s) 주기로 사이트 자동 반영(재배포 불필요).
> 🔒 **TODO(보안):** 셋업 중 토큰이 채팅에 1회 노출됨 → 노션에서 토큰 재발급 후 `.env.local`·Vercel 값 교체 권장.

### 완료된 것
- [x] GitHub 연결 (baekwebpage, `main`)
- [x] 구현 계획 수립 (`구현계획.md`)
- [x] 브랜드명 확정 ("백관장의 체력 상담소")
- [x] 디자인 방향 확정 (남색 `#004AAD` + IBM Carbon)
- [x] 멀티페이지 시안 (`디자인시안/` home·columns·products + style.css)
- [x] 자가진단 미끼 기획 = 5단계 밸류 래더 + 통증 2축 (`자가진단테스트_계획.md`)
- [x] 보안/Vercel/자체결제/로그인 검토 보고 (2단계 전략: 1차 외부링크 → 2차 Supabase 커머스)
- [x] 콘텐츠 윤문 단계(`/humanize-korean`) 도입
- [x] **P0 코드: Next.js(App Router/TS) 앱 구성** — `app/`(layout·page·columns·products·globals.css), `components/`(Header·Footer), `public/`(이미지). 시안 CSS 그대로 재사용. `npm run build` 성공, 로컬 `npm run dev` 정상.

### 다음 할 일
- [x] **Vercel 배포 완료** → https://masterbaek.vercel.app (검증: 3페이지·이미지·콘텐츠 정상)
- [x] Notion Integration 토큰 발급 + DB 연결 (완료, `.env.local` 저장)
- [x] **P2 빌드 검증 → 로컬 확인 → 커밋** (빌드 7/7 성공, 로컬·라이브 렌더 검증)
- [x] **Vercel 환경변수 등록 + 배포** (vercel CLI로 link·env·배포. 라이브 검증 통과)
- [x] **문의 폼 실전송 — B안(Web3Forms) 완료 (2026-07-06)** — 키 발급(사용자)→`.env.local`+Vercel(`NEXT_PUBLIC_WEB3FORMS_KEY`, Prod·Dev)→`ContactForm.tsx` mailto→Web3Forms POST 교체·배포. 번들에 키 임베드 확인. 무료 월 250건, Web3Forms엔 저장 안 됨(메일 전달만 — 네이버 메일함이 기록). ✅ **종단 테스트 완료**: 첫 테스트는 키 발급 이메일(지메일)로 수신 → Web3Forms에서 수신 이메일을 네이버로 교체(키 동일, 코드 무변경) → **네이버 도착 확인(2026-07-06)**. ※ Tally API는 제출 API 없음(2026-07-06 확인) → 커스텀 폼 유지엔 Web3Forms가 유일했음. 도메인 구매 후 Resend+브랜드 메일 이전 예정
- [ ] **뉴스레터 의견 폼 = Tally로 제작** (사용자 예정) — 스티비 레터 메일 안에 Tally 폼 링크 삽입 용도. 사이트 코드 작업 불필요
- [ ] ⏭️ **실제 칼럼 가공(marketingteamforbaek 에이전트로) → 발행** (다음 1순위)
- [ ] (선택) 토큰 재발급 — 보안 TODO
- [ ] P3 상품 상세 / P6 자가진단 데모 / 문항·결과문 초안

### 🔧 P2 작업 요약 (이번 세션 — 재부팅 후 참고)
**설계 변경:** "노션 원문 그대로 노출" 폐기 → **AI 1차 가공 → 백관장 감수 → 윤문 → 발행** 구조. 상세 스펙: [`docs/superpowers/specs/2026-06-07-칼럼-콘텐츠-파이프라인-design.md`](./docs/superpowers/specs/2026-06-07-칼럼-콘텐츠-파이프라인-design.md)

> 🔎 **스티비 발행물 직접 연동 검토(2026-06-08) → 보류 결정.** easystrength101.stibee.com에 발행물 20+편 공개(/p/번호)지만 ① RSS·공식 API 없음(headless 스크래핑만 가능, 취약) ② 본문이 이메일 HTML(중첩 table)+수신거부 등 잔재 ③ **공지·전자책 세일즈 글이 칼럼과 혼재**(예 /p/14는 제목만 칼럼, 내용은 전자책 할인 메일). → 자동 직결 시 공지·만료 프로모션이 칼럼으로 둔갑. **결론: 노션 중심 현행 유지.** 재활용 원하면 스티비를 '소스 풀'로 보고 선별·가공 후 노션 발행(반자동)으로 전환 가능.
- **이유:** 노션 `발행완료`·`뉴스레터 출력` 글들은 전부 영상 대본·메모 수준이라 사이트에 그대로 못 씀. 재가공 필수.
- **실제 운영 가공은 `marketingteamforbaek` 레포의 뉴스레터 제작 에이전트로** 한다 (이번 세션의 샘플은 형태 미리보기용).

**만든 것:**
1. **노션 신규 DB "홈페이지 칼럼"** 생성 — 속성: 제목/요약/slug/상태(초안·감수중·발행완료)/등급(A·B)/날짜
   - DB id: `d12f20fd785446049fe50cc49ab366bf` / data source id: `1dfe4ef9-09de-42f3-a5f0-8d197b64ea23`
   - `발행완료` 상태만 사이트 노출 (감수 게이트). 등급 A = 홈 추천.
2. **토큰 발급 + DB 연결 완료** → `.env.local`에 `NOTION_TOKEN`·`NOTION_COLUMNS_DB_ID`·`NOTION_COLUMNS_DS_ID` 저장 (git 제외). curl 쿼리 테스트 통과.
3. **샘플 칼럼 1편** DB에 발행완료로 넣음 — "헬스장 처음 가면 런닝머신만 타고 오는 게 정상입니다" (slug: `gym-first-month`, 등급 A).
4. **연동 코드 작성:**
   - `lib/notion.ts` — REST API 직접 호출(의존성 0). `getColumns`/`getColumnBySlug`/`getBlocks`/`formatDate`. Notion-Version `2025-09-03`, ISR revalidate 60s.
   - `components/NotionContent.tsx` — 노션 블록(문단/헤딩/리스트/인용/구분선) → 사이트 디자인 렌더.
   - `app/columns/page.tsx` — 더미 제거, 노션 연동(추천 A=feature, 나머지=grid).
   - `app/columns/[slug]/page.tsx` — 신규 상세 페이지(남색 표지 헤더 + 본문 + 하단 상품 CTA).
   - `app/page.tsx` — 홈 칼럼 섹션도 노션 최신 3편 연동.
   - `app/globals.css` — 표지 카드(`.fcover`/`.ccover`, 그림 없이 남색+제목) + 상세 본문(`.article*`) 스타일 추가.

**미검증/다음 확인 포인트:** `npm run build` 끝까지 통과 여부(이번 세션엔 빌드 도중 재부팅). 빌드 시 노션 API 호출로 다소 느릴 수 있음 — 멈춘 게 아님.

### 기술 메모 (P0)
- 스택: Next.js 15 + React 19 + TypeScript. **Tailwind는 초안에서 미사용** — 승인된 시안 CSS(`app/globals.css`)를 그대로 써서 디자인 1:1 재현·속도 우선. 추후 필요 시 Tailwind 도입 가능.
- 실행: `npm run dev`(로컬), `npm run build`(빌드). node_modules/.next는 .gitignore 처리.
- 이미지: `public/`의 logo.png·profile.jpg·hyundai-id.png를 `<img src="/...">`로 참조.

### 미결정/대기
- 도메인 미정 (Vercel 임시주소로 시작)
- 진행 방향 A(2단계 전략) vs B(풀스펙) — A 추천, 최종 확정 대기
- Notion Integration 토큰 발급 (P2 전에 필요)
- 이메일 서비스(스티비) 계정 (P6 전에 필요)

---

### 로그
- **2026-07-08 (2)** — ✅ **②만14세+개인정보 동의 체크박스 (이메일 폼 3곳)**: 개인정보 수집 폼 전부(`SubscribeForm`·`Quiz`·`ContactForm`)에 `만 14세 이상이며 [개인정보 수집·이용](/privacy)에 동의합니다` **필수 체크박스** 추가 — 네이티브 `<input type="checkbox" required>`라 미체크 시 브라우저가 제출 차단(별도 상태관리 없음, fetch 미실행). `/privacy`는 새 탭(입력 보존). CSS 공용 `.consent`(`color:inherit`로 다크밴드[뉴스레터]·라이트 폼 양쪽 대응) + `.quiz-email`에 `flex-wrap:wrap`(체크박스 줄바꿈). `tsc` 통과. **6개 항목 중 ①🔴·②🟡 해소 완료.** ⏭️ 남은 건 ⑥ 스티비 발송설정 점검(코드 밖).
- **2026-07-08 (1)** — ✅ **법적 컴플라이언스 감사(6개 항목) + 개인정보처리방침 신설**: 인용 조문에 코드 대조 감사 → 판정: **①처리방침 없음=🔴위반(실제 리스크)**, ②만14세 확인 없음=🟡부분, ④비밀번호 평문로그=🟢해당없음(로그인/비번 자체 없음), ⑤위치정보 무신고=🟢해당없음(geolocation 코드 전무, 네이버 지도는 링크일 뿐), ⑥수신동의=🟡부분(스티비 더블옵트인으로 동의근거 확보✅, 스티비 발송템플릿 (광고)표기·수신거부·야간동의만 점검). 사이트 수집=이메일 3곳(문의 Web3Forms / 뉴스레터·자가진단 스티비)뿐, 로그인·결제·위치 기능 없음. **→ ①번 처리**: `app/privacy/page.tsx` 신설(제30조 필수항목 전수: 수집항목·목적·보유기간·처리위탁+국외이전[스티비 국내/Web3Forms·Vercel 미국]·쿠키[Vercel Analytics]·정보주체 권리·파기·안전성·보호책임자[백승덕 qortmdejr123@naver.com]·권익침해 구제기관·시행일 2026-07-08), `Footer.tsx`에 `/privacy` 링크 추가. `.sg-pagehead`+`.sg-article` 스타일 재사용. `tsc --noEmit` 통과. **윤문 생략**(법적 보일러플레이트=표준 문구 유지가 정확). ⏭️ 남은 권장: ②이메일 폼 2곳에 만14세+동의 체크박스, ⑥스티비 발송설정 점검. 사업자등록 시 처리방침에 상호·주소 추가 가능.
- **2026-07-07 (1)** — ✅ **SEO/OG 전수 감사 + 트래킹 설치 가이드 작성**: 바이럴 "전문가 기본세팅" 프롬프트 항목을 라이브에서 실측 — OG(전 페이지+칼럼별 동적 이미지 렌더 200)·페이지별 타이틀·canonical·sitemap·robots·JSON-LD·Vercel Analytics(insights script 200=대시보드 활성) **전부 이미 완료 상태 확인, 코드 변경 불필요.** 유일한 공백=GSC·네이버 서치어드바이저 등록(계정 권한 필요, 코드는 환경변수 훅으로 준비됨) → [`docs/트래킹-설치-가이드.md`](./docs/트래킹-설치-가이드.md) 신설(서치콘솔·네이버 각 5분 스텝바이스텝, GA4·메타픽셀은 광고 시점으로 보류). ⏭️ 사용자: 가이드 1·2번 실행(15분).
- **2026-07-06 (4)** — ✅ **문의 폼 Web3Forms 실전송 완료(B안)**: 사용자가 키 즉시 발급 → `.env.local`·Vercel 환경변수(`NEXT_PUBLIC_WEB3FORMS_KEY`) 등록 → `ContactForm.tsx` mailto 제거, Web3Forms POST(전송중/접수완료/실패폴백 UI + honeypot + 문의유형에 온라인 강의 추가) → 배포. 검증: 라이브 JS 청크에 web3forms 호출·키 임베드 확인(HTML엔 안 보임 — 클라이언트 번들에 인라인). curl 테스트는 무료 플랜 서버전송 차단+Cloudflare 챌린지로 불가(정상 — 브라우저 전용 정책). ⏳ 사용자: /contact 테스트 제출 1건→네이버 메일 도착 확인. 참고: Web3Forms는 저장 없음(메일 전달만), Tally는 제출 API 없음(공식 문서 확인)이라 커스텀 폼 유지엔 Web3Forms 선택이 맞았음. 뉴스레터 의견 폼은 Tally로 별도(코드 작업 불필요).
- **2026-07-06 (3)** — ✅ **홈 다크 상품 리스트에 《출근력》 추가**: 전자책(01)→**출근력(02)**→원데이(03)→정규수업(04)로 재배열 — 9,900원 전자책과 오프라인 수업 사이 가격 사다리(₩249,000) 확보. 📌 **문의 폼 Web3Forms(1안) 확정 — 사용자가 이번 주말 진행 예정**, 상세 절차는 [다음 할 일] 체크리스트에 기록.
- **2026-07-06 (2)** — ✅ **온라인 강의 실상품 연결 + 교보 1위 문구**: ① '온라인 강의(준비중)' → **클래스101 《출근력》 실판매 상품**으로 교체(`online-course`: ₩249,000·5단계 커리큘럼[진단→준비→3대운동+케틀벨→75%법칙→통증관리]·buyUrl=class101.net/ko/products/6874f8b9837426ba1f6db2fa). 준비중 상품은 이제 운동 프로그램·굿즈 2개. ※ 클래스101 페이지의 "8년/5만" 숫자는 구버전이라 미채용(사이트 기준 10년/6.2만 유지). 할인가는 프로모션 변동이라 정가만 표기. ② **교보문고 건강 분야 베스트 1위**(2025.09 일간, 사용자 캡처 확인) — 홈 저서 스트립("정식 출간 저서"→"교보문고 건강 분야 1위")·About 저서 줄·`book-survival` short/tagline/detail 반영. ③ 새 카피 humanize 윤문 통과(변경률 0%). `tsc` 통과. 브랜드명은 "백관장의 체력 상담소" 유지 확정(사용자). ⏭️ 문의 폼 서버 전송(Web3Forms/Resend) 검토 보고 — 사용자 결정 대기.
- **2026-07-06 (1)** — ✅ **사이트 전수 점검 → 개선 일괄 반영** (사용자 승인 후): ① **히어로 카피 논리 수정**(A안: "쓰러지는 건 체력 문제 → 방법 알면 다시 만든다", 문제공감 마무리 '정상 아님/당연함' 충돌 해소, '혹시' 중복 제거). ② **책 표지 배치** `public/book-cover.jpg`(사용자 제공, 324×480) — 홈 저서 스트립 빈 박스 해소. ③ **깨진 앵커 `/#sns` 제거** — 준비중 상품 상세에 `SubscribeForm`(제목/설명 props 추가) 배치 + `#notify` 앵커로 교체. ④ **사실 정합**: 코치 경력 10년 통일(전자책 8년→10년), About "2권 정식 출간"→"정식 출간 1·전자책 1", 히어로 아이브로 "직장인 체력 전문 · 유튜브 6.2만"(브랜드명은 "백관장의 체력 상담소" 유지 — "백관장의 직장인 체력 상담소"는 길어서 비권장, 사용자 확인 대기). ⑤ **후기**: prof****(바벨 4주 세미나 후기가 원데이로 잘못 배치) 삭제, 대표 후기 choi****로 교체. ⑥ **칼럼 피처 주간 자동 로테이션**(`Date.now()/주` mod — 등급 A는 더 이상 피처 선정에 안 쓰임). ⑦ **자가진단**: '주의' CTA `/products/ebook` 딥링크 + **결과 화면 이메일 수집 복원**(스티비 `/api/subscribe` 재사용, 등급→`체력유형` 필드). ⑧ **근력학교 정규수업 대폭 개편**: 신청 링크 → 블로그 `blog.naver.com/qortmdejr123/223306435526`(실시간 시간표), 상세 본문을 블로그 기반 재작성(김X상 사례·배터리 비유·환불 "첫 수업 후 한 달 이내 100%"로 정정·입학반 포함), 오프라인 상품에 네이버 지도 링크(`naver.me/xiqDtNuY`)+주소 표기. ⑨ **상품 목록**: 준비중 상품(온라인강의·프로그램·굿즈) 카드→"출시 알림 받기" 행 리스트로 축소(상세의 알림 폼 연결). ⑩ 장식 번호(01·02·03) `aria-hidden`. 새 카피는 humanize-monolith 윤문 통과(변경률 2%). `tsc` 통과. 💡 **발견**: 블로그에 클래스101 강의 "[출근력]" 존재 — 사이트 '온라인 강의(준비중)'와 연결할지 사용자 결정 필요. 문의 폼은 mailto 방식(방문자 메일앱에서 보내야 도착) — Resend 등 서버 전송 전환은 추후.
- **2026-07-01 (1)** — ✅ **홈 저자·신뢰 스트립 추가**(권위 자연스럽게 보강, 설계: [`docs/superpowers/specs/2026-07-01-홈-저자신뢰스트립-design.md`](./docs/superpowers/specs/2026-07-01-홈-저자신뢰스트립-design.md)). 오리진 스토리 바로 뒤에 하드라인 스트립 1개: 책 표지(좌) + `▍저서 · 백관장은` / 《최소한의 생존 체력 운동》 / **직장인 체력 전문 코치 · 근력학교 이사 · 유튜브 6.2만** / 저서 보러가기 → (`/products/book-survival` 카드형 링크). 스토리 서명도 `— 백관장(백승덕) · 근력학교 이사`로 보강. 표지는 **배경이미지 방식**(`public/book-cover.jpg`, 없으면 빈 박스로 graceful) — 사용자 파일 배치 대기. 브레인스토밍→스펙 승인 후 구현. `.sg-author*` CSS 추가(중괄호 481/481). `tsc` 통과. ⏭️ **사용자 할 일: 책 표지를 `public/book-cover.jpg`로 저장** 후 커밋(넣으면 자동 표시).
- **2026-06-30 (4)** — ✅ **소개(About) 장문 서사 복원**: (3)에서 시안 간결판으로 덮어쓰며 빠졌던 기존 긴 카피(현대중공업 물근육·데드리프트 140kg·3대 540kg·계단 숨참·케틀벨 전환·IPF 1등·차별점·원칙 4카드·숫자 4지표+저서·변화)를 **전부 복원**하되 새 에디토리얼(`.sg-*`) 디자인으로 재구성. 구성: hero split → 문제공감(`sg-pain`) → **다크 반전 스토리 6단락**(`sg-narr.dark`) → 차별점(`sg-narr`) → 원칙 2×2 카드(`sg-cards`) → 숫자(`sg-bigstats`)+저서 → 변화 → **PHILOSOPHY 밴드**(보너스) → 시그니처 인용(보너스) → CTA. (3) 정리 때 삭제했던 `.sg-narr/cards/sechead/books/id` CSS 재추가(중괄호 470/470). 검증 `tsc` 통과. ※ 원본 카피는 git `422f0e9:app/about/page.tsx`에도 보존.
- **2026-06-30 (3)** — ✅ **서브페이지를 페이지별 시안(design_handoff v2)에 정합 재구현**: 사용자가 핸드오프에 페이지별 `.dc.html`(칼럼/상품/소개/문의/자가진단) + README v2를 추가 → 직전(홈 디자인 일반화) 버전을 **각 시안대로 다시 구현**. ① **칼럼**: 추천=틴트 박스, 전체=행 리스트(`1fr auto`, 제목+발췌/우측 모노 날짜·hover 파랑), 뉴스레터 **다크 밴드**(`SubscribeForm` 다크+▍NEWSLETTER), split CTA. ② **상품**: 2열 **카드**(모노 카테고리코드 EBOOK/BOOK/…+가격, 준비중 muted, 오프라인 strong 보더), 추천 박스(자가진단/상담), GOODS 행, 다크 CTA. ③ **소개**: 시안의 **간결판**으로 교체(hero split→스토리01/02→다크 PHILOSOPHY 밴드→4지표 strip[6.2만/534+/8년/540kg]→시그니처 인용→footer CTA). *기존 장문 서사(540kg·IPF·케틀벨 전환·원칙카드)는 시안이 간결화 방향이라 대체됨 — git 이력에 보존.* ④ **문의**: split(정보 0.85 / **문의 폼** 1.15). 폼=`components/ContactForm.tsx` 신설, 메일 백엔드 부재로 **mailto 전송**(이름·이메일·유형·메시지). ⑤ **자가진단**: `components/Quiz.tsx`를 시안의 **7문항 인터랙티브**(intro→질문→결과 상태머신, 진행바, tierFor 양호/주의/위험→칼럼·상품 CTA)로 전면 교체. *기존 3축 퀴즈+스티비 이메일 수집은 제거(뉴스레터 수집은 칼럼 페이지 `SubscribeForm`에 유지).* ⑥ 정리: 구 3축 퀴즈 CSS·직전 턴의 대체된 `.sg-cards/narr/statgrid/sechead/email` 등 제거(`.quiz-email`만 유지). 검증: **`tsc --noEmit` 통과**·CSS 343→442 중괄호 균형·삭제 클래스 잔여 참조 0. 로컬 빌드는 iCloud I/O로 불가 → Vercel 게이트. ⚠️ 문의 폼은 mailto v1 — 서버리스 메일(Resend 등) 붙이면 `ContactForm` submit만 교체.
- **2026-06-30 (2)** — ✅ **전체 서브페이지 "Structured Grid" 통일**: 홈에 이어 나머지 모든 페이지를 동일 에디토리얼 디자인(모노 라벨·하드라인·`#0A4DB0`/`#15171C`·1180 컨테이너)으로 재구성. ① `globals.css`에 서브페이지 공용 `.sg-*` 어휘 추가(`sg-pagehead`·`sg-catlabel`·`sg-cards`·`sg-feature`·`sg-rows/row`·`sg-cta`·`sg-art-hero/article`·`sg-prod-hero/body/pmain/pbuy`·`sg-narr`·`sg-statgrid`·`sg-email`·`sg-sechead`·`sg-h1.sm`)+`.review-*` retoken+반응형. ② 페이지 변환(카피·링크·데이터·로직 보존): `/columns`(피처+하드라인 카드그리드)·`/columns/[slug]`(에디토리얼 아티클, `.reading` 유지)·`/products`(카테고리별 **넘버링 행 리스트**)·`/products/[id]`(에디토리얼 상세+구매박스)·`/about`(서사 전면 재구성: split 히어로→문제행→**다크 스토리**→narrow 섹션→2×2 원칙카드→스탯그리드→CTA)·`/contact`(이메일+채널 행)·`/quiz`·`404`(헤더). ③ **레거시 CSS ~180줄 삭제**(구 hero/origin/page-head/feature/col-card/cover/article/grid-prod/prod/product-detail/ab-*/cta-band) → 단일 디자인 시스템화. 인터랙티브 컴포넌트(Quiz·SubscribeForm)는 이미 각진 네이비라 내부 유지, 상품·문의의 이모지 아이콘은 에디토리얼 정합 위해 제거. 검증: **`tsc --noEmit` 통과**·CSS 중괄호 343/343·삭제 클래스 잔여 참조 0. 로컬 `next build`은 iCloud I/O로 불가 → Vercel 게이트. 9개 파일.
- **2026-06-30 (1)** — ✅ **홈 리디자인 "Structured Grid"(디자인 핸드오프 적용)**: `design_handoff_homepage/`의 최종안 C(비대칭 에디토리얼 그리드 + 모노 넘버링)대로 홈 전면 재구성. ① `app/page.tsx` 재작성(히어로 split → 스탯 스트립 → 문제공감 → 오리진 스토리 → 칼럼[노션 연동 유지] → 후기[lib/reviews] → **다크 상품 리스트** → 푸터 CTA). ② `app/globals.css`에 `--sg-*` 토큰 + `.sg-*` 스타일 추가(기존 서브페이지 CSS와 네임스페이스 분리, 1px 하드라인 에디토리얼 룩·≤860px 1열 반응형). ③ 공용 `Header`(1.5px ink 보더·모노 "자가진단 →")·`Footer`(로고+이메일/저작권 하단바) 갱신 → 사이트 크롬 일관화. ④ `layout.tsx`에 **Space Mono**(모노 라벨/넘버링) 추가, 한글 본문은 기존 **IBM Plex Sans KR 유지**(핸드오프 권고: 코드베이스 폰트 우선). 검증: **`tsc --noEmit` 통과**(로컬 `next build`/`dev`는 iCloud I/O로 불가 — Vercel이 빌드 게이트). **범위=홈 전용**(서브페이지 본문은 기존 IBM Carbon 룩 유지 → 홈과 시각 차이 발생, 전사 확산은 추후). 잔여: Pretendard 미적용(IBM Plex 유지)·구 홈 전용 CSS(`.hero`/`.origin`/`.cta-band`/`.stats`) 비활성 잔존(추후 삭제 가능). 디자인 핸드오프 번들도 함께 커밋.
- **2026-06-29 (3)** — ✅ **칼럼 표지 카드 정렬 + real-stamina-test 본문 보강**: ① `.ccover`(칼럼 그리드 남색 표지) `align-items:flex-end`→`center`. 짧은 제목일 때 위쪽 빈칸이 커 제목이 아래로 처져 보이던 문제 해소(홈·칼럼 목록 공통). ② 노션 칼럼 `real-stamina-test`(러닝 기록이 빨라져도…) 마지막 단락 "그다음부터는 세트나 무게를 올려 가면 됩니다."→"30세트를 넘게 해도 180공식 심박수를 넘지 않으면, 무게를 한 단계 올려서 진행하는 걸 추천합니다(남 +4kg, 여 +2kg)." (사용자 지정 보강, ISR 자동반영). CSS는 커밋·푸시→Vercel 배포.
- **2026-06-29 (2)** — ✅ **주간 칼럼 자동화(클라우드 루틴) 세팅**: 매주 칼럼 초안을 자동 생성하도록 claude.ai 스케줄 루틴 등록. ① 노션 `홈페이지 칼럼` 페이지 아래 **`칼럼 소스함` DB 신설**(`collection://7180eb42-3e60-42ca-8ff5-c48aea9e3d30`, 속성: 제목·상태[미가공/가공완료]) — 영상대본/메모를 페이지 본문에 쌓아두는 입력함. ② **루틴 `trig_01JoouFgQrPSE6puVfnWvZHj`** (cron `0 0 * * 1` = 매주 월 09:00 KST, 모델 sonnet-4-6, MCP=Notion+Slack 연결). 동작: 소스함 가장 오래된 `미가공` 1편 → 백관장 톤 칼럼 재가공(AI 티 제거·내용보존·렌더 지원 블록만) → `홈페이지 칼럼 DB`에 **상태=초안·등급=B**로 생성 → 소스 `가공완료` 처리 → **Slack DM 알림**. 안전장치: 발행완료 자동전환 금지(검토 게이트 유지)·미가공 없으면 "소스 채워달라" DM만·사실 생성 금지·1회 1편. 파이프라인의 `초안(AI)` 단계만 자동화, **감수→윤문→발행은 사용자 수동**. 관리: https://claude.ai/code/routines/trig_01JoouFgQrPSE6puVfnWvZHj · 첫 실행 2026-07-06(월). ⏭️ 첫 실행 전 소스함에 1편 넣고 "Run now"로 종단 테스트 권장.
- **2026-06-29 (1)** — ✅ **칼럼 읽기 UX**: 칼럼 상세에서 sticky 상단 네비(72px)가 스크롤해도 떠 있어 읽을 공간이 줄던 문제. 칼럼 상세를 `.reading` 래퍼로 감싸 해당 페이지에서만 `header`를 `position:static`(본문과 함께 스크롤). 홈·상품 등 마케팅 페이지의 고정 네비+CTA는 유지. 커밋 `e7e3727`. 라이브 검증(`class="reading"`·CSS 규칙 배포 확인).
- **2026-06-28 (1)** — ✅ **SEO/측정 4건 + 칼럼 12편 발행 + 메뉴 추가**:
  - **SEO 코드(커밋 `e29f3a5`)**: ① `@vercel/analytics`(`<Analytics/>` layout) — 퍼널 측정 시작. ② **JSON-LD**(`components/JsonLd.tsx` 신설, `</script>`만 이스케이프) — 전역 Organization/Person/WebSite(@graph, sameAs 5채널) + 칼럼 Article + 상품 Product+Offer(`priceKRW` 파서, 자가검증 통과). ③ **canonical** 8개 페이지 전부(`alternates.canonical`). ④ **sitemap** `available` 상품만 색인(준비중 thin page 제외). 보너스: GSC·네이버 소유확인을 **환경변수 훅**(`GOOGLE_SITE_VERIFICATION`·`NAVER_SITE_VERIFICATION`)으로 미리 깔아둠 → 코드 받으면 Vercel 환경변수만 채우면 끝. `tsc` 통과(`next build`는 iCloud 행 → Vercel이 게이트).
  - **AggregateRating 보류**: 실제 평점 숫자가 없어 날조 대신 보류 — 네이버지도/스마트스토어 판매자센터의 **평균 평점·건수** 받으면 그때 추가.
  - **칼럼 6→18편**: `3. 백관장 마케팅 팀/outputs` 미발행 뉴스레터 12편을 **병렬 서브에이전트 12개**로 사이트 톤 변환(`SPEC.md`에 보이스+윤문 규칙 인라인 적용)→ 노션 발행완료. 날짜는 **원본 소스일(2/20~6/26)로 분산**해 덤프 신호 회피, 자가진단 3축(근력·유산소·생활/회복)+케틀벨 상품 커버. 중복 slug 없음. `why-no-energy-after-work`만 등급 A. (마크다운→노션블록 발행 스크립트는 scratchpad `publish.mjs`.)
  - **메뉴**: `Header.tsx`에 "백관장에게 직접 배우기"(외부 근력학교 신청 URL) 추가 → **같은 날 철회**(사용자 판단). 근력학교 정규수업(`school`)이 이미 그 신청 링크(`timetablemanager?register=true`)에 연결돼 있어 별도 메뉴 불필요. ('원데이 9만원 신설' 요청도 기존 `oneday`(케틀벨 원데이, ₩90,000, 동일 링크)와 중복이라 신설 안 함.)
  - ⚠️ **다음 사용자 작업(계정 권한 필요)**: 도메인 구매(vercel.app 탈출=최대 SEO 천장) · GSC/네이버 서치어드바이저 등록(→환경변수 채우기) · AggregateRating용 실제 평점.
  - ⚠️ **윤문**: 12편은 변환 시 humanize 규칙을 인라인 적용(별도 `/humanize-korean` 패스 생략). 추가 손질 원하면 가능.
- **2026-06-26 (3)** — ✅ **About 카피 정정 + 자가진단 3축 재설계**: ① About 연대기 정정(대회는 코치 전향 **후**로 이동, "대회1등인데 계단숨참"→"540kg 드는 사람이 계단숨참"으로 후킹 유지, 히어로·홈 오리진 정합) + 원칙·시장(보디빌딩/다이어트)·저서표기·"하루10분→주2~3회" 문구 수정. ② **자가진단 전면 재설계**(`Quiz.tsx`): 단일 총점(0~24) → **3축 채점(근력·유산소·생활/회복, 각 4문항=12 + 통증 3)**. 결과는 **'가장 약한 고리'** 진단형(3축 막대 시각화, 약점축→상품 추천: 근력약점=전자책/유산소약점=케틀벨원데이/회복약점=전자책, 전부 바닥=방전형/전부 상위=균형강철형). 채점 로직 순수JS 10/10 케이스 검증. ③ 결과 이메일 "전체 리포트+맞춤 루틴"(vaporware) → 정직한 "체력 레터"로. 커밋 `b04ccee`(무게)·`62b5235`(About카피)·`7cf57c9`(자가진단). 라이브 검증 완료(문항 15개·빌드 통과). ⚠️ 통증 분기는 여전히 체력 점수를 덮음(의도). '운동 프로그램'(준비중) 추천은 이번에 안 쓰도록 정리됨.
- **2026-06-26 (2)** — ✅ **P2 마감 3건**: ① **폰트** Google Fonts `<link>`(외부 렌더블로킹)→`next/font/google` IBM Plex Sans KR 자체호스팅(`layout.tsx` `--font-ibm` 변수, `globals.css` body 적용) — FOUT·외부요청 제거. ② **커스텀 404**(`app/not-found.tsx`) — 기본 영문 404 대신 브랜드 페이지(홈·칼럼·자가진단 동선). ③ **정직성**(`Quiz.tsx` 통증 결과) — 미출시 "통증 케어 콘텐츠 출시 알림"(vaporware) 제거 → 실제 상품 **근력학교 정규수업(1단계 교정=통증 잡기)** 추천 + 실제 체력레터 구독으로 대체. 3커밋(`54c84eb`·`e28f6e6`·`125fbe7`). ⚠️ 폰트 next/font 설정은 로컬 빌드 불가라 Vercel 빌드로 검증. 남은 P2: 굵은글씨 점검(변경 불필요로 판단). 통증 결과의 근력학교 추천은 마케팅 판단이라 톤 조정 원하면 알려줄 것.
- **2026-06-26 (1)** — ✅ **홈 퍼널·성능 개선 4건(계획서: [`docs/superpowers/plans/2026-06-26-홈-퍼널-개선.md`](./docs/superpowers/plans/2026-06-26-홈-퍼널-개선.md))**: ① **성능** 첫 화면 이미지 경량화 — profile.jpg 14.8MB→161KB(리사이즈), 사원증 hyundai-id.png 3.8MB→178KB(PNG→JPEG, `sips`), 참조 2곳 `.png`→`.jpg`(`app/page.tsx`·`app/about/page.tsx`). 총 18.7MB→0.34MB(98%↓). ② **UX** 모바일 햄버거 메뉴 신설(`Header.tsx` client 토글 + `globals.css` `.nav-toggle`/`.menu.open` 드롭다운) — ≤900px에서 `.menu{display:none}`로 사라지던 네비 복구. ③ **안정성** `lib/notion.ts` 3함수(`getColumns`/`getColumnBySlug`/`getBlocks`) try/catch — 노션 장애 시 빈 폴백(홈은 "칼럼 준비 중"으로), 사이트 전체 500 방지. ④ **전환** 홈을 허브→피치로 재배치(`app/page.tsx`): 히어로 리드 아픔먼저, 히어로 직후 "문제 공감" 블록(about 카피 재사용), 오리진에 "대회 1등인데 계단 5층 숨참" 반전 후킹, 칼럼 프레이밍 + **"매주 새로" 미검증 발행빈도 주장 제거**, 종료 CTA 밴드에 자가진단/상품 버튼 추가. 4커밋(`2a4bda5`·`7cfb99f`·`57c6c33`·`21ac49a`). ⚠️ **로컬 빌드 검증 불가** — 프로젝트가 iCloud(Desktop 동기화) 위라 `next build`·`tsc`가 `node_modules`/`.next` I/O에서 0% CPU로 행. 코드 정합성은 육안 확인, **실빌드는 Vercel(클린 환경)이 게이트.** 근본 해결은 프로젝트를 iCloud 밖으로 이동 권장.
- **2026-06-13 (3)** — ✅ **상품 정보 실페이지 기준 정정 + 홈페이지 전체 윤문**: ① 전자책《직장인을 위한 케틀벨 체력 솔루션》9,900원·무료샘플, 원데이 9만원·케틀벨 커리큘럼(스윙·터키쉬겟업·힙힌지)·6명·복습영상+전자책, 정규수업 3단계(교정→근력→유산소)·주2회31만/3회39만/4회45만·입학반8만필수·24h100%환불 — 모두 실제 판매/예약 페이지(리틀리·github.io·네이버블로그) 확인 후 정정. 홈 상품카드도 동기화. ② **`/humanize-korean`(humanize-korean@im-not-ai v1.5.0, 이미 설치됨) 윤문**: humanize-monolith 에이전트로 About 전체 서사·홈 카피·`lib/products.ts` 설명문·상품/칼럼/문의 페이지 인트로·CTA·자가진단 결과문·구독폼까지 일괄 윤문(변경률 5~25%, 의미·숫자·고유명사·전자책 제목 보존). **`lib/reviews.ts`(실제 후기)·가격·브랜드명·슬로건은 윤문 제외.** About 카피 윤문 완료(이전 미적용분 해소). 빌드 통과. ⚠️ 홈/칼럼 "매주 새로 올라옵니다"는 칼럼 발행 빈도 주장 — 실제 발행 페이스와 맞는지 추후 확인 권장.
- **2026-06-13 (2)** — ✅ **2순위 후기 섹션 + 오프라인 상품 신청 URL 연결**: ① 사용자가 직접 모아준 후기로 `lib/reviews.ts` 구축(네이버 지도=근력학교 6건 + 스마트스토어=케틀벨 원데이 6건, 작성자 이니셜 익명화·발췌 인용·의미보존). `components/Reviews.tsx`(별점·태그·출처 배지 카드 그리드, prod-bg 배경·반응형 3→2→1열). 홈은 상품 섹션 앞에 대표 6건 노출(근력학교·원데이 믹스), 상품 상세(school·oneday)는 해당 상품 후기만 필터 노출. ② **신청 URL 연결**(스마트스토어 대신 사용자 자체 예약 시스템): 근력학교 정규수업→`baekstrong.github.io/timetablemanager/?register=true`, 케틀벨 원데이→`baekstrong.github.io/productdetailpage/`. via 라벨 "온라인 신청"으로 변경, buyUrl null 해제(이제 둘 다 버튼 활성). 빌드 통과, 홈/상품상세 렌더·URL 검증 완료. **→ lib/products.ts에 남아있던 buyUrl TODO 2건 모두 해소.**
- **2026-06-13 (1)** — ✅ **신뢰 보강 1~4순위 일괄 진행** (isanghan.co.kr 벤치마크 검토 후): ① SNS 5채널 실링크(유튜브·인스타·스레드·틱톡·블로그)+실제 문의 이메일+홈 칼럼 수 노션 발행 수 동적 카운팅. ② **후기 자동 수집 검토 → 불가 판정**(스마트스토어 curl 429 차단, 브라우저는 네이버 캡차 — 캡차 우회는 안 함) → 판매자센터 수동 복사 or 공식 커머스API 대안 제시, 사용자 후기 원문 대기. ③ **P5 SEO/OG**: metadataBase+OG/트위터 메타, OG 이미지 자동 생성(사이트 공통+칼럼별 동적, next/og+IBM Plex KR 서브셋 `lib/og.ts`), favicon(app/icon.png=로고), sitemap.ts(노션 칼럼 포함)·robots.ts. ④ 저서 《최소한의 생존 체력 운동》 상품 추가(교보문고 19,800원)+전자책 리틀리 직링크 교체, 뉴스레터 구독 폼(SubscribeForm, 스티비 더블옵트인 재사용) 칼럼 목록·상세 하단 배치, `/contact` 문의 페이지 신설(헤더 문의→/contact). API: 일반 구독 시 통증여부 미기록. 빌드 28라우트 통과, OG 렌더 검증. ⚠️ `.next`가 iCloud 동기화로 `columns 2` 충돌 복제된 적 있음 — 빌드가 이상하면 `rm -rf .next` 후 재빌드.
- **2026-06-05 (1)** — 프로젝트 시작. GitHub 연결 → 기획(구현계획/자가진단) → 브랜드명 확정 → 디자인 시안(멀티페이지) → 보안·인프라 검토 → 윤문 단계·진행상황 관리 규칙 도입.
- **2026-06-05 (2)** — P0 진행: 시안을 Next.js(15)로 전환. 홈/칼럼/상품 3페이지 + 공유 Header/Footer 컴포넌트화. 빌드 성공, 로컬 작동 확인.
- **2026-06-05 (3)** — ✅ P0 완료: Vercel 배포(https://masterbaek.vercel.app). 3페이지·이미지·콘텐츠 정상 검증. 이후 main push 시 자동배포.
- **2026-06-07 (1)** — 🔧 P2 진행: 칼럼 파이프라인 재설계(노션 원문 직접노출 폐기 → AI가공+감수 구조, 스펙 문서화·커밋). 노션 "홈페이지 칼럼" DB 신설 + 토큰 발급/연결 + 샘플 1편 발행완료. 연동 코드 작성 완료(lib/notion·NotionContent·칼럼 목록/상세/홈·표지·본문 CSS). **빌드 검증 직전 재부팅** — 재부팅 후 `npm run build`부터 이어가기. (커밋 아직 안 함, 코드는 디스크 저장됨)
- **2026-06-07 (2)** — ✅ **P2 완료**: 재부팅 후 빌드 검증(7/7 성공) → 로컬 렌더 확인 → 칼럼 하단 CTA 문구 완화(판매색 제거) → 커밋(`6069bd1`). vercel CLI(npx)로 프로젝트 link + 환경변수 등록(`NOTION_TOKEN`·`NOTION_COLUMNS_DS_ID`, Prod·Dev) + `main` push 자동배포. 라이브(https://masterbaek.vercel.app/columns) 목록·상세·홈·완화 CTA 전부 검증 통과. **노션 칼럼 → 사이트 자동연동 실운영 가동.**
- **2026-06-07 (3)** — ✅ **실제 칼럼 5편 + P3 상품상세 + P6 자가진단(데모)**: ① `marketingteamforbaek`(="3. 백관장 마케팅 팀" 레포) `outputs/` 뉴스레터 9편 중 5편을 병렬 서브에이전트로 **영상대본 톤 → 사이트 칼럼 재가공** 후 노션 DB에 발행완료(8가지습관[A]·숨참4가지·7단계시스템·작심삼일·케틀벨횟수). ② **P3**: `lib/products.ts`(상품 6종 데이터·외부결제) + 상품목록 리팩터 + `/products/[id]` 상세 신설(전자책=리틀리 실링크, 스마트스토어 상품 URL은 `buyUrl:null`=미정). ③ **P6**: `/quiz` 자가진단(체력 8문항+통증 3문항, 브라우저 즉시 채점, 통증/유형 분기 결과+상품추천), 헤더·홈 진입동선 연결. 이메일 수집은 스티비 계정 전이라 UI/데모(제출 시 완료 메시지만). 빌드 14/14 통과.
  - ⚠️ **남은 입력 필요**: (1) 스마트스토어 원데이·근력학교 상품 URL → `lib/products.ts`의 `buyUrl` (2) 스티비 계정 → `/quiz` 이메일 실연동 (3) 칼럼 5편 발행 전 윤문 미적용(원하면 적용 가능).
- **2026-06-07 (4)** — ✅ **스티비 연동 + 윤문 완료**: ① `/api/subscribe`에 스티비 v1 구독 연동, 키 발급·`.env.local`·Vercel(Prod·Dev) 등록·재배포·라이브 테스트(`{ok:true}`) 통과. 보안리뷰 3건 반영(에러 노출 제거·더블옵트인·이메일 검증). listId 463893, 더블옵트인(confirmEmailYN=Y). 커스텀필드 `체력유형`·`통증여부`. ② 칼럼 5편 `/humanize-korean`(humanize-korean 플러그인) 윤문 — 변경률 8~12%, 의미·수치·구조 보존, 노션 본문 `replace_content`로 교체(ISR 자동반영). **남은 건 스마트스토어 결제 URL 입력뿐.**
  - 🔒 보안 TODO: 노션 토큰·스티비 키가 셋업 중 채팅에 노출됨 → 추후 둘 다 재발급 권장.
- **2026-06-07 (5)** — ✅ **소개(About) 페이지 신설** — 두 저서(《최소한의 생존 체력 운동》·《직장인 케틀벨》) 프롤로그 검토 → isanghan.co.kr식 설득 랜딩 구조로 `/about` 긴 서사 작성(반전 후킹→문제·공감→오리진 스토리→직장인 출신 차별점→원칙 4카드→숫자 증거→변화→자가진단 CTA). 홈 origin에 "전체 보기" 링크, 헤더 소개→`/about` 연결. 사원증·프로필 사진 배치. 빌드 21/21. ⏭️ **about 카피 윤문 미적용**(JSX라 사용자 검토 후 적용 예정).
