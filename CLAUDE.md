# CLAUDE.md — 백관장의 체력 상담소 (홈페이지 프로젝트)

> 이 파일은 작업 시 항상 따라야 할 규칙과 프로젝트 핵심 정보를 담는다.
> 상세 구현 계획은 [`구현계획.md`](./구현계획.md) 참고.

---

## ⚠️ 작업 규칙 (필수)

1. **작업 시작 전 — 항상 `git pull`** 먼저 실행해 최신 상태를 받는다.
2. **작업 완료 후 — 항상 `git add` → `git commit` → `git push`** 한다.
3. 커밋 메시지는 한국어로 명확하게 (무엇을 왜 바꿨는지).
4. 발행/배포 등 되돌리기 어려운 작업은 진행 전 사용자에게 확인한다.

```bash
# 작업 시작
git pull

# 작업 완료
git add .
git commit -m "변경 내용 요약"
git push
```

- GitHub: https://github.com/baekstrong/baekwebpage (`main` 브랜치)

---

## 프로젝트 개요

**무엇:** 백관장(백승덕)의 체력향상 콘텐츠 허브 + 상품 판매 퍼널 홈페이지
**벤치마킹:** https://awesomebliss.kr/

**목적 (퍼널):**
```
SNS 유입(유튜브·스레드·인스타·틱톡) → 칼럼으로 설득 → 상품 구매 전환
```
어썸블리스와 차이: **칼럼(콘텐츠)을 퍼널의 중심**에 둔다.

---

## 브랜드

- **사이트 브랜드명**: **백관장의 체력 상담소** (채널명 계승, 구독자 6.2만 인지도 활용)
  - ※ "근력학교"는 **정규수업 상품명**일 뿐, 사이트 브랜드명이 아님
- **운영자**: 백관장(백승덕) — **전직 현대중공업 엔지니어** (사원증 = 오리진 스토리 핵심 자산)
- **슬로건**: "직장인에게, 힘을!"
- **미션**: 스트렝스 트레이닝의 대중화
- **타깃**: 직장인 — 바쁜 와중에 체력/근력을 키우려는 사람
- **보이스**: 직설적·현실적·경험기반·친근하되 전문적 (군더더기 없음)
- **채널**: @easystrength101 (유튜브 구독 6.18만)

---

## 기술 스택 (확정)

| 영역 | 선택 |
|------|------|
| 프레임워크 | Next.js (App Router) + TypeScript |
| 스타일 | Tailwind CSS |
| 칼럼 CMS | **Notion** (기존 발행 흐름 그대로) |
| 배포 | **Vercel** (git push 자동배포, 무료) |
| 결제/판매 | **외부 플랫폼 링크** (자체 결제 X) |
| 도메인 | 없음 → Vercel 임시주소로 시작, 추후 구매 |

---

## 칼럼 자동 발행 구조 (핵심)

**사용자 작업 흐름은 바뀌지 않는다.** 지금처럼 Notion에 발행만 하면 된다.

```
원본(영상대본/메모)
  → marketingteamforbaek 에이전트가 백관장 톤 뉴스레터로 변환
  → Notion 발행용 DB(30f68eb32d9b809e8477f11b3b9b4a37) 발행
  → 상태: 검토 대기 → (사용자 감수) → 발행 완료   ← 감수 게이트
  → 홈페이지가 "발행 완료"만 자동으로 읽어옴 (ISR)
  → 사이트에 칼럼으로 노출
```

- Notion 속성 매핑: 제목→칼럼제목 / 날짜→정렬 / **상태=`발행 완료`만 노출** / 등급 A=홈 추천칼럼 / 본문→렌더링
- 별도 레포: https://github.com/baekstrong/marketingteamforbaek (뉴스레터 변환·발행)
- **콘텐츠 자산(이미 존재)**: 뉴스레터 ~18편(칼럼 1순위) + 블로그 ~60편(지역 SEO, 선별 활용)
- 사전 준비(사용자 1회): Notion Integration 토큰 발급 + 발행용 DB 연결 → Vercel 환경변수 저장

---

## 결제 플랫폼 (상품별 구매 버튼 목적지)

| 상품 | 결제 목적지 |
|------|------------|
| 전자책 | **리틀리** (litt.ly/easystrength) |
| 원데이 오프라인 강의 | **스마트스토어** |
| 근력학교 정규수업 | **스마트스토어** |
| 온라인 강의 (10편 이하) | 미정 |
| 운동 프로그램 | 미정 |
| 실물 굿즈 | 추후 |

---

## 디자인 시스템

- **컨셉**: 브랜드 남색 + IBM Carbon = "신뢰감 있고 군더더기 없는 전문가 사이트"
- **메인 색**: Brand Navy **`#004AAD`** (로고서 추출, 흰색 로고와 한 쌍)
  - 호버 `#003A87` / 액티브 `#002D6E` / 틴트 `#E6EEF8`
  - 텍스트 `#161616` / 보조 `#525252` / 보더 `#c6c6c6` / 면 `#f4f4f4` / 배경 `#fff`
  - 에너지 액센트(소량) Red `#DA1E28`
- **폰트**: IBM Plex Sans KR (무게 300/400/600 위주)
- **레이아웃**: 8px 그리드, 모서리 0px, 그림자 최소(배경 계층화), 컨테이너 ~1200px, 모바일 우선

---

## 사이트맵

```
/                  홈 (랜딩)
/columns           칼럼 목록 (Notion 자동 생성)
/columns/[slug]    칼럼 상세 (+ 하단 관련 상품 CTA)
/products          상품 전체
/products/[id]     상품 상세 (구매 → 외부 플랫폼)
/about             백관장 소개 (현대중공업 오리진 스토리)
/contact           문의 (이메일 / SNS)
```

---

## 자산 (assets/)

- `assets/logo.png` — 흰색 "백관장" 로고 (남색 배경)
- `assets/profile.jpg` — 백승덕 헬스장 프로필 사진
- `assets/hyundai-id.png` — 현대중공업 사원증 (오리진 스토리용)

---

## 진행 현황

- [x] GitHub 연결 (baekwebpage)
- [x] 구현 계획 수립 (`구현계획.md`)
- [x] 브랜드명 확정 ("백관장의 체력 상담소")
- [x] 디자인 방향 + 홈 시안 (`디자인시안/home.html`)
- [ ] 시안 확정
- [ ] P0: Next.js 셋업 + Vercel 자동배포
- [ ] P1: 홈 / P2: Notion 칼럼 연동 / P3: 상품 / P4: 소개·문의 / P5: 마감

---

## 구현 단계 (Phase)

| 단계 | 내용 |
|------|------|
| P0 | Next.js + Tailwind 셋업, Vercel 연결, 자동배포 확인 |
| P1 | 디자인 시스템 + 홈 랜딩 |
| P2 | Notion 칼럼 연동 (목록/상세, ISR 자동갱신) |
| P3 | 상품 목록/상세 + 외부 결제 링크 |
| P4 | 소개/문의 + SNS 링크 |
| P5 | SEO/OG/성능/도메인 연결 |
